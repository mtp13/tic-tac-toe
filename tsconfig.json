{
  "compilerOptions": {
    "allowJs": true, // Allow JavaScript files in the project.
    "checkJs": false, // Disable type checking for JavaScript files.
    "noEmit": true
  },
  "exclude": ["node_modules"] // Explicitly exclude JavaScript files from type-checking.
}
